<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  {{CAMERA_PERMISSION}}
  <application
    android:allowBackup="false"
    {{CLEARTEXT_ATTR}}
    android:label="{{APP_NAME}}"
    {{ICON_ATTR}}
    android:theme="@style/Theme.AppCompat.NoActionBar"
  >
    <activity
      android:name="aio.app.MainActivity"
      android:exported="true"
      android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboard|keyboardHidden"
    >
      <!-- configChanges: a rotation (or a keyboard) must not recreate the
           activity — that destroyed the WebView and RELOADED the app, losing
           everything on screen not yet saved. The WebView resizes itself. -->
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
