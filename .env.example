# aio environment variables — copy to .env and fill in values

# Application identity (required at runtime)
# APP_ID=my-app
# APP_VERSION=1.0.0

# TLS (optional — for HTTPS/WSS server)
# TLS_CERT_FILE=./certs/cert.pem
# TLS_KEY_FILE=./certs/key.pem

# Port (optional — defaults to 8080)
# PORT=8080
