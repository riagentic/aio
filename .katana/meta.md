# Meta kata for aio

- deno.json `version`, the CHANGELOG's top released section, and the newest git
  tag all name the same version
- git commits are squashed into fewer more meaningful commits to avoid bloated
  git log history
